name: pipeline
on:
  schedule:
    - cron: "17 3 * * *"   # daily 03:17 UTC
  workflow_dispatch:
concurrency:
  group: pipeline
  cancel-in-progress: false
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GH_PAT }}   # real identity so the push triggers downstream
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Run deterministic pipeline
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          # Enables autonomous LLM curation of the discovered backlog. If the secret is
          # unset, auto-curation is skipped and the run still publishes human + first-party
          # entries (makeLlmCurator() returns null without a key).
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: npm run pipeline
      # On push to main, Vercel's git integration auto-deploys the new catalog — no explicit hook step.
      - name: Commit artifacts if changed
        run: |
          git config user.name "aleph-hub-bot"
          git config user.email "bot@heyaleph.com"
          git add public/catalog.json data/
          if git diff --cached --quiet; then
            echo "no changes to commit"
          else
            git commit -m "chore(catalog): refresh artifact $(date -u +%Y-%m-%d)"
            git push
          fi
